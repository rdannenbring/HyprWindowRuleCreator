#!/usr/bin/env python3
"""Launcher that works from a git checkout without installing."""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))

from hyprwrc.cli import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
